import { useState } from "react";
import axios from "axios";

const useApplicationData = () => {


  //-----------------------------------LOG------------------------------------//

  // // Function to log a workout
  // const logWorkout = async (data) => {
  //   try {
  //     const response = await axios.post("/log", data);
  //     return response.data; // 
  //   } catch (error) {
  //     console.error("Error logging workout:", error);
  //     return null; //
  //   }
  // };

  // // Function to get workout log for a specific user
  // const getWorkoutLog = async (userId) => {
  //   try {
  //     const response = await axios.get(`/log/${userId}`);
  //     return response.data; //
  //   } catch (error) {
  //     console.error("Error getting workout log:", error);
  //     return null; // Return null or handle the error in some way
  //   }
  // };

//---------------------------------------------------------------------------//
};

export default useApplicationData;